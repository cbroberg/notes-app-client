language: node_js

node_js:
- node

notifications:
    email: true
    slack: webhouseteam:82qu1PX5R5wkYNCW5x6fVlhu

cache:
  directories:
    - node_modules

script: 
    - npm run build
    - aws s3 sync build/ s3://react-notes-app-client
    - aws cloudfront create-invalidation --distribution-id EX72Z0FEP8XSI --paths '/*' && aws cloudfront create-invalidation --distribution-id EX72Z0FEP8XSI --paths '/*'
